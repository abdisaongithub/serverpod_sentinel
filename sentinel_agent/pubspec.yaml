name: sentinel_agent
description: A standalone system monitoring agent for Serverpod Sentinel.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  serverpod_client: ^3.2.2
  system_info2: ^4.0.0
  yaml: ^3.1.2
  logging: ^1.2.0
  watcher: ^1.1.0
  archive: ^3.6.1
  serverpod_sentinel_client:
    path: ../serverpod_sentinel_client

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
